package com.example.service.support.views

import org.assertj.core.api.Assertions
import org.junit.jupiter.api.Test

internal class IdFieldTest {
    @Test
    fun `값이 없으면 0`() {
        //   Assertions.assertThat(IdField().value).isZero()
    }

    @Test
    fun `유효한 값을 입력하는 경우`() {

    }
}